## REVIEW: this qmd + the underlying dir should be moved to the inst/ folder for demo space - R/ should only contain code functionality

```{r}
library(purrr)
library(tidyverse)
library(stringr)

# dvs_status("dir/*") %>% filter(status != "up-to-date") %>% pull(path) %>% dvs_get()

dvs_status("dir/*") %>% filter(status == "not-present" | status == "out-of-sync") %>% pull(path) %>% dvs_get()
```


```{r}
status <- list(errors = data.frame(), successes = data.frame())
if (!nrow(status$errors)) {
  print(status$errors)
}
status$successes %>% filter(status != "up-to-date") %>% pull(path) %>% dvs_get()
```



